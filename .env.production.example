# Production / staging deploy secrets (set in Coolify, Vercel, etc.)
# Do not use these values locally during active schema development.

DATABASE_URL=postgresql://user:password@your-vps-host:5432/vashabid_prod?sslmode=require
PAYLOAD_SECRET=your-production-secret
DATABASE_PUSH=false
